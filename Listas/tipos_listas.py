# lista preenchida estaticamente
lista_estatica = ["xpto", True]

# lista preenchida dinamicamente
lista_dinamica = [input("Digite um usuário: "), bool(int(input("Está logado? ")))]

# lista vazia
lista_vazia = []

# obs1: em todos os casos os valores devem estar entre [], mesmo a lista sendo vazia.

# obs2: na lista dinamica o segundo item passa por duas conversões:
# a primeira para transformar a string (valor natural do input/sempre retorna string) em um int
# a segunda para transformar o int em um boolean (True / False)

